services:
    frontend:
        build:
            context: .
            dockerfile: Dockerfile
            args:
                BUILD_ENV: dev
        container_name: dev-frontend
        ports:
            - "4200:80"   # browse at http://localhost:4200
