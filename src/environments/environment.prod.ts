import {version} from '../../package.json';

export const environment = {
    production: true,
    appVersion: version,
    baseUrl: 'https://ehbconnect.app',
    apiBaseUrl: 'https://api.ehbconnect.app',
};
